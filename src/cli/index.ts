/**
 * Re-exports `cli-args-parser` so every dynamic-openapi consumer stays on the
 * same version without declaring the dep themselves. Add new names here as the
 * upstream package grows — do not add runtime code in this module.
 */
export * from 'cli-args-parser'
